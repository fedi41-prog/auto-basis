radio.onReceivedValue(function (name, value) {
    if (verbunden == 0) {
    	
    } else if (false) {
    	
    }
})
let verbunden = 0
verbunden = 0
let code = 4512652
radio.setGroup(128)
radio.setTransmitSerialNumber(true)
